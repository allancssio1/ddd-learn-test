export class Product {
  public id: string
  public name: string
  public size: string
  public color: string

  constructor(id: string, name: string, size: string, color: string) {
    this.id = id
    this.name = name
    this.size = size
    this.color = color
  }
}
